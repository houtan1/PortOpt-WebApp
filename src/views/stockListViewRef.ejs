<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Title: <%= title %></title>
  </head>
  <body>
    <h1>
      Choose a pair of stocks!
    </h1>
    <!-- start a form here with simple get (not post) -->
    <form action="/pair" method="get">
      <!-- Loop through and display the stocks -->
      <% for(var i = 0; i < stock.length; i++) { %>
        <h2>
          <%= stock[i].name %>
        </h2>
        <p>
          <a href="info/<%= i %>">Expectation and Risk for <%= stock[i].name %>
          </a>
        </p>
        <p>
          <input type="checkbox" name="index" value="<%= i %>">
            Select <%= stock[i].name %> for your portfolio.
        </p>
      <% } %>
      <input type="submit" value="Submit Stock Pair">
    </form>
  </body>
</html>
